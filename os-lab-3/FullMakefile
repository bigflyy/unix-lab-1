obj-m += tsulab.o 
//  переменная системы сборки ядра Linux, указывающая, какие файлы должны быть скомпилированы как модули  Указывает, какие объектные файлы должны быть скомпилированы как модули (а не как часть основного ядра)

PWD := $(CURDIR) // текущий рабочий каталог
all:
	make -C /lib/modules/$(shell uname -r)/build M=$(PWD) modules
clean: Эта цель очищает все скомпилированные файлы, аналогично цели all, но вызывает команду clean
	make -C /lib/modules/$(shell uname -r)/build M=$(PWD) clean

all: - стандартная цель (target) по умолчанию при запуске make
make -C - запустить make из указанного каталога
/lib/modules/$(shell uname -r)/build - путь к исходникам ядра текущей версии:
uname -r возвращает версию запущенного ядра
M=$(PWD) - указывает системе сборки ядра, где находятся наши исходные файлы
modules - цель для сборки модулей

